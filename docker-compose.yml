services:
  browser-service:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - API_KEY=your-secret-key        # 建议修改
      - MIN_BROWSERS=2                  # 最少保持的浏览器数
      - MAX_BROWSERS=10                 # 最多并发浏览器数
      - PREWARM_PAGES=true              # 每个浏览器预建一个独立页面，以额外内存降低响应延迟
      - MAX_FETCH_FILE_BYTES=52428800    # /fetch-file 单次文件最大字节数
      - PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
    deploy:
      resources:
        limits:
          memory: 2g                    # 根据服务器内存调整
    restart: unless-stopped
    shm_size: '1gb'                     # 共享内存，防止 Chrome 崩溃
